{
  "manifest_version": "0.3",
  "name": "zenodo-mcp-server",
  "version": "0.1.0",
  "description": "Search and resolve Zenodo datasets, software, and publications by DOI; trace versions and funding, list files, and preview text files. Keyless. STDIO or Streamable HTTP.",
  "author": { "name": "cyanheads" },
  "repository": {
    "type": "git",
    "url": "https://github.com/cyanheads/zenodo-mcp-server.git"
  },
  "homepage": "https://github.com/cyanheads/zenodo-mcp-server#readme",
  "license": "Apache-2.0",
  "server": {
    "type": "node",
    "entry_point": "dist/index.js",
    "mcp_config": {
      "command": "node",
      "args": ["${__dirname}/dist/index.js"],
      "env": {
        "MCP_TRANSPORT_TYPE": "stdio",
        "MCP_AUTH_MODE": "none",
        "NODE_ENV": "production",
        "ZENODO_ACCESS_TOKEN": "${user_config.zenodo_access_token}"
      }
    }
  },
  "compatibility": {
    "runtimes": { "node": ">=24.0.0" }
  },
  "tools_generated": true,
  "prompts_generated": true,
  "user_config": {
    "zenodo_access_token": {
      "type": "string",
      "title": "Zenodo Access Token",
      "description": "Optional Zenodo personal access token for higher rate limits (100 req/min vs 30 req/min, larger page sizes). Generate one at https://zenodo.org/account/settings/applications/tokens/new/.",
      "required": false,
      "sensitive": true,
      "default": ""
    }
  }
}
